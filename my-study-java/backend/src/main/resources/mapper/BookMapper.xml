<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.library.mapper.BookMapper">

  <resultMap id="BookResult" type="com.example.library.model.Book">
    <id property="id" column="id" />
    <result property="title" column="title" />
    <result property="author" column="author" />
    <result property="description" column="description" />
  </resultMap>

  <select id="findAll" resultMap="BookResult">
    SELECT id, title, author, description FROM book
  </select>

  <select id="findById" parameterType="long" resultMap="BookResult">
    SELECT id, title, author, description FROM book WHERE id = #{id}
  </select>

  <insert id="insert" parameterType="com.example.library.model.Book" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO book (title, author, description)
    VALUES (#{title}, #{author}, #{description})
  </insert>

  <update id="update" parameterType="com.example.library.model.Book">
    UPDATE book SET title=#{title}, author=#{author}, description=#{description} WHERE id=#{id}
  </update>

  <delete id="delete" parameterType="long">
    DELETE FROM book WHERE id = #{id}
  </delete>

</mapper>
